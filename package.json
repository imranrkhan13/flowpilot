{
  "name": "flowpilot",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix web run build",
    "test": "npm --prefix server test && npm --prefix web test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix web run typecheck",
    "dev": "concurrently -n api,web -c blue,green \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "start": "npm --prefix server start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
